import { createRoot } from 'react-dom/client';
import './index.css';
import App from './App.jsx'; // Ensure correct import path and file extension

createRoot(document.getElementById('root')).render(
  <App />
);

